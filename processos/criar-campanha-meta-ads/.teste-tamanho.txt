<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Processo · Criar campanha no Meta Ads · Brito Mídias</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&amp;display=swap" rel="stylesheet">
<style>
  /* Paleta declarada inteira no :root — nenhum literal de cor fora daqui,
     exceto #fff/#000 e os rgba() de sombra e de grade do hero. */
  :root{
    --roxo:#7C3AED; --roxo-dark:#5B21B6; --roxo-deep:#3B0F73;
    --roxo-light:#A78BFA; --roxo-xlight:#EDE9FE; --roxo-xxlight:#FCFAFF;
    --preto:#0A0A0A; --hero-mid:#17111F; --ink:#141018; --cinza:#5B5566;
    --linha:#E7E3EE; --bg:#FFFFFF; --bg-soft:#FAF9FC;
    --verde:#16A34A; --verde-dark:#15803D; --verde-bg:#F0FDF4;
    --amarelo:#D97706; --ambar-dark:#92400E; --ambar-soft:#FEF3C7; --ambar-bg:#FFFBEB;
    --vermelho:#DC2626; --code-fg:#E9E4F5;
  }
  *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
  body{font-family:'Poppins',system-ui,-apple-system,sans-serif;font-weight:300;font-size:16px;
    line-height:1.6;color:var(--ink);background:var(--bg);-webkit-font-smoothing:antialiased}
  a{color:inherit}
  .wrap{max-width:1000px;margin:0 auto;padding:0 28px}

  /* HERO */
  .hero{position:relative;overflow:hidden;color:#fff;
    background:
      radial-gradient(1100px 500px at 78% -10%, rgba(124,58,237,.55), transparent 60%),
      linear-gradient(150deg,var(--preto) 0%,var(--hero-mid) 45%,var(--roxo-deep) 100%);}
  .hero::before{content:"";position:absolute;inset:0;
    background-image:
      linear-gradient(rgba(255,255,255,.14) 1px,transparent 1px),
      linear-gradient(90deg,rgba(255,255,255,.14) 1px,transparent 1px);
    background-size:44px 44px;
    -webkit-mask-image:radial-gradient(circle at 50% 30%,#000 25%,transparent 72%);
    mask-image:radial-gradient(circle at 50% 30%,#000 25%,transparent 72%);pointer-events:none}
  .hero-in{position:relative;padding:40px 0 50px}
  .brand{display:flex;align-items:center;gap:16px}
  .brand svg{width:46px;height:46px;flex-shrink:0}
  .brand .word{display:flex;align-items:center;gap:14px;font-weight:300}
  .brand .word b{font-weight:800;font-size:20px}
  .brand .word .bar{width:2px;height:30px;background:rgba(255,255,255,.55)}
  .brand .word span{font-size:20px;letter-spacing:.22em}
  .crumb{margin-top:32px;display:flex;align-items:center;gap:8px;flex-wrap:wrap;
    font-size:12px;font-weight:600;letter-spacing:.1em;text-transform:uppercase;color:var(--roxo-light)}
  .crumb b{color:#fff}
  .crumb .sep{color:rgba(255,255,255,.35);font-weight:400;letter-spacing:0;text-transform:none}
  .hero h1{font-weight:800;font-size:clamp(28px,4.4vw,44px);letter-spacing:-.02em;line-height:1.15;
    margin-top:12px;text-wrap:balance}
  .hero .sub{font-weight:300;font-size:16.5px;color:rgba(255,255,255,.74);margin-top:14px;max-width:62ch}
  .pills{display:flex;flex-wrap:wrap;gap:10px;margin-top:24px}
  .pill{border:1px solid var(--roxo-light);color:#fff;border-radius:100px;padding:8px 16px;
    font-size:12px;font-weight:600;letter-spacing:.12em;text-transform:uppercase}
  .pill.solid{background:linear-gradient(90deg,var(--roxo),var(--roxo-dark));border-color:transparent}

  /* SEÇÕES */
  .sec{padding:52px 0}
  .sec.soft{background:var(--bg-soft);border-top:1px solid var(--linha);border-bottom:1px solid var(--linha)}
  .section-label{display:flex;align-items:center;gap:12px;font-size:13px;font-weight:600;
    letter-spacing:.16em;text-transform:uppercase;color:var(--roxo)}
  .section-label::before{content:"";width:34px;height:2px;background:var(--roxo);border-radius:2px}
  .sec h2{font-weight:700;font-size:clamp(22px,3vw,28px);letter-spacing:-.01em;margin-top:14px;text-wrap:balance}
  .sec .intro{color:var(--cinza);margin-top:12px;max-width:70ch}
  .sec .intro code{background:var(--roxo-xlight);color:var(--roxo-dark);padding:2px 6px;border-radius:5px;
    font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12.5px;overflow-wrap:anywhere}
  .sec .intro strong{color:var(--ink);font-weight:600}
  .sec h3.mini{font-size:16px;font-weight:700;margin-top:34px;letter-spacing:-.01em}

  .callout{border:1px solid var(--linha);border-left:4px solid var(--roxo);border-radius:12px;
    padding:20px 24px;margin-top:22px;background:#fff}
  .callout.amber{border-left-color:var(--amarelo);background:var(--ambar-bg)}
  .callout.green{border-left-color:var(--verde);background:var(--verde-bg)}
  .callout h3{font-size:15px;font-weight:700;margin-bottom:8px}
  .callout p{color:var(--cinza);font-size:14.5px}
  .callout p+p{margin-top:9px}
  .callout strong{color:var(--ink);font-weight:600}
  .callout ul{margin-top:10px;padding-left:20px;color:var(--cinza);font-size:14.5px}
  .callout li{margin-top:5px}
  .callout code{background:var(--roxo-xlight);color:var(--roxo-dark);padding:2px 7px;border-radius:6px;
    font-size:13px;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;overflow-wrap:anywhere}
  .callout a{color:var(--roxo-dark);font-weight:600}

  /* ROADMAP (fases) */
  .road{display:grid;grid-template-columns:repeat(2,1fr);gap:14px;margin-top:26px}
  .fase{border:1px solid var(--linha);border-radius:14px;padding:18px 20px;background:#fff;
    display:grid;grid-template-columns:38px 1fr;gap:14px;align-items:start}
  .fase .num{width:38px;height:38px;border-radius:11px;
    background:linear-gradient(135deg,var(--roxo),var(--roxo-dark));color:#fff;
    display:flex;align-items:center;justify-content:center;font-weight:800;font-size:15px;
    box-shadow:0 8px 16px rgba(124,58,237,.24)}
  .fase h3{font-size:15.5px;font-weight:700;letter-spacing:-.01em}
  .fase p{color:var(--cinza);font-size:13.5px;margin-top:5px}
  .fase .done{display:block;margin-top:9px;font-size:12px;font-weight:600;color:var(--verde-dark)}
  .fase.hot{border-color:var(--roxo-light);background:var(--roxo-xxlight)}

  /* TABELA DE NOMENCLATURA */
  .nome-ex{margin-top:18px;border:1px solid var(--linha);border-left:4px solid var(--roxo);
    border-radius:12px;background:var(--bg-soft);padding:14px 18px;
    font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:13px;color:var(--roxo-dark);
    overflow-wrap:anywhere;line-height:1.75}
  .ntab{margin-top:14px;border:1px solid var(--linha);border-radius:12px;overflow:hidden}
  .nrow{display:grid;grid-template-columns:200px 1.1fr 1.4fr;gap:16px;padding:13px 18px;font-size:13.5px}
  .ntab.duo .nrow{grid-template-columns:200px 1fr}
  .nrow:nth-child(odd){background:var(--bg-soft)}
  .nrow b{color:var(--roxo-dark);font-family:ui-monospace,SFMono-Regular,Menlo,monospace;
    font-size:12.5px;overflow-wrap:anywhere}
  .nrow span{color:var(--ink);font-weight:400}
  .nrow em{color:var(--cinza);font-style:normal}
  .nrow.head{background:var(--roxo-xlight);font-size:12px;font-weight:700;letter-spacing:.1em;
    text-transform:uppercase;color:var(--roxo-dark)}
  .nrow.head span,.nrow.head em{color:var(--roxo-dark);font-weight:700}

  /* CAMPOS (config) */
  .fields{margin-top:16px;border:1px solid var(--linha);border-radius:12px;overflow:hidden}
  .frow{display:grid;grid-template-columns:200px 1fr;gap:14px;padding:13px 18px;font-size:13.5px}
  .frow:nth-child(odd){background:var(--bg-soft)}
  .frow b{color:var(--roxo-dark);font-family:ui-monospace,SFMono-Regular,Menlo,monospace;
    font-size:12.5px;overflow-wrap:anywhere}
  .frow span{color:var(--cinza)}
  .frow span strong{color:var(--ink);font-weight:600}

  /* PASSO */
  .step{display:grid;grid-template-columns:60px 1fr;gap:20px;margin-top:40px}
  .step>.body{min-width:0}
  .step:first-of-type{margin-top:24px}
  .step .idx{width:60px;height:60px;border-radius:16px;flex-shrink:0;
    background:linear-gradient(135deg,var(--roxo),var(--roxo-dark));color:#fff;
    display:flex;align-items:center;justify-content:center;font-weight:800;font-size:24px;
    box-shadow:0 10px 22px rgba(124,58,237,.28)}
  .step .body h3{font-size:19px;font-weight:700;letter-spacing:-.01em}
  .step .body p{color:var(--cinza);font-size:14.5px;margin-top:7px}
  .step .body p strong{color:var(--ink);font-weight:600}

  /* CORREÇÕES DO GESTOR */
  .fix{border:1px solid var(--linha);border-radius:14px;background:#fff;margin-top:16px;overflow:hidden}
  .fix .cab{display:flex;align-items:center;gap:12px;padding:14px 20px;background:var(--bg-soft);
    border-bottom:1px solid var(--linha)}
  .fix .cab .n{width:26px;height:26px;border-radius:8px;background:var(--roxo);color:#fff;flex-shrink:0;
    display:flex;align-items:center;justify-content:center;font-size:13px;font-weight:800}
  .fix .cab h3{font-size:15px;font-weight:700}
  .fix .par{display:grid;grid-template-columns:1fr 1fr}
  .fix .cel{padding:16px 20px;font-size:14px;color:var(--cinza)}
  .fix .cel+.cel{border-left:1px solid var(--linha)}
  .fix .cel .lab{display:block;font-size:12px;font-weight:700;letter-spacing:.1em;text-transform:uppercase;
    margin-bottom:6px}
  .fix .cel.erro .lab{color:var(--vermelho)}
  .fix .cel.regra{background:var(--verde-bg)}
  .fix .cel.regra .lab{color:var(--verde-dark)}
  .fix .cel strong{color:var(--ink);font-weight:600}
  .fix code{background:var(--roxo-xlight);color:var(--roxo-dark);padding:2px 7px;border-radius:6px;
    font-size:12.5px;font-family:ui-monospace,SFMono-Regular,Menlo,monospace}

  /* CHECKLIST */
  .check{margin-top:22px;border:1px solid var(--linha);border-radius:14px;overflow:hidden;background:#fff}
  .check li{list-style:none;display:grid;grid-template-columns:24px 1fr;gap:14px;align-items:start;
    padding:14px 20px;font-size:14.5px;color:var(--ink)}
  .check li:nth-child(odd){background:var(--bg-soft)}
  .check li::before{content:"";width:20px;height:20px;margin-top:2px;border-radius:6px;
    border:2px solid var(--roxo-light);background:#fff;display:block}
  .check li strong{font-weight:600}
  .check li em{display:block;font-style:normal;color:var(--cinza);font-size:13px;margin-top:3px}

  /* ARMADILHAS */
  .traps{display:grid;grid-template-columns:repeat(2,1fr);gap:14px;margin-top:24px}
  .trap{border:1px solid var(--linha);border-radius:14px;padding:18px 20px;background:#fff}
  .trap .n{font-size:12px;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:var(--roxo)}
  .trap h3{font-size:15.5px;font-weight:700;margin-top:6px;letter-spacing:-.01em}
  .trap p{color:var(--cinza);font-size:13.5px;margin-top:7px}
  .trap p strong{color:var(--ink);font-weight:600}
  .trap code{background:var(--roxo-xlight);color:var(--roxo-dark);padding:2px 6px;border-radius:5px;
    font-size:12px;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;overflow-wrap:anywhere}
  .trap.red{border-left:4px solid var(--vermelho)}

  .obs{display:flex;gap:16px;align-items:flex-start;border:1px solid var(--linha);
    border-left:4px solid var(--amarelo);background:var(--ambar-bg);border-radius:12px;padding:20px 24px;margin-top:32px}
  .obs .ic{font-size:24px;flex-shrink:0}
  .obs h3{font-size:15px;font-weight:700;color:var(--ambar-dark);margin-bottom:6px}
  .obs p{color:var(--cinza);font-size:14.5px}
  .obs p+p{margin-top:9px}
  .obs strong{color:var(--ink);font-weight:600}
  .obs code{background:var(--ambar-soft);color:var(--ambar-dark);padding:2px 7px;border-radius:6px;font-size:12.5px;
    font-family:ui-monospace,SFMono-Regular,Menlo,monospace;overflow-wrap:anywhere}

  pre.code{margin-top:14px;background:var(--ink);color:var(--code-fg);border-radius:12px;padding:16px 18px;
    font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12.5px;line-height:1.7;
    overflow-x:auto;white-space:pre}

  footer{background:var(--ink);color:rgba(255,255,255,.62);padding:40px 0}
  footer .brand svg{width:34px;height:34px}
  footer .brand .word b{font-size:15px}footer .brand .word span{font-size:15px}
  footer .brand .word .bar{height:22px}
  footer .fnote{margin-top:16px;font-size:12.5px;max-width:80ch;line-height:1.7}
  footer a{color:var(--roxo-light);text-decoration:none;border-bottom:1px solid rgba(167,139,250,.4)}

  @media (max-width:640px){
    .wrap{padding:0 18px}
    .road{grid-template-columns:1fr}
    .traps{grid-template-columns:1fr}
    .nrow,.ntab.duo .nrow{grid-template-columns:1fr;gap:6px}
    .nrow.head{display:none}
    .nrow span::before{content:"O que significa: ";font-weight:600;color:var(--roxo-dark)}
    .nrow em::before{content:"Por que existe: ";font-weight:600;color:var(--roxo-dark)}
    .frow{grid-template-columns:1fr;gap:5px}
    .step{grid-template-columns:1fr;gap:12px}
    .step .idx{width:48px;height:48px;font-size:20px;border-radius:13px}
    .fix .par{grid-template-columns:1fr}
    .fix .cel+.cel{border-left:0;border-top:1px solid var(--linha)}
  }
</style>
</head>
<body>

<!-- HERO -->
<header class="hero">
  <div class="wrap hero-in">
    <div class="brand">
      <svg viewBox="0 0 100 100" fill="none" stroke="#fff" stroke-width="2.4" role="img"
           stroke-linejoin="round" stroke-linecap="round" aria-label="BM Mídias">
        <polygon points="50,4 90,27 90,73 50,96 10,73 10,27"></polygon>
        <polygon points="50,33 65,59 35,59"></polygon>
        <line x1="50" y1="4" x2="50" y2="33"></line><line x1="90" y1="73" x2="65" y2="59"></line>
        <line x1="10" y1="73" x2="35" y2="59"></line><line x1="90" y1="27" x2="50" y2="33"></line>
        <line x1="90" y1="27" x2="65" y2="59"></line><line x1="50" y1="96" x2="65" y2="59"></line>
        <line x1="50" y1="96" x2="35" y2="59"></line><line x1="10" y1="27" x2="35" y2="59"></line>
        <line x1="10" y1="27" x2="50" y2="33"></line>
      </svg>
      <span class="word"><b>BM</b><i class="bar"></i><span>MÍDIAS</span></span>
    </div>

    <div class="crumb"><b>Brito Mídias</b><span class="sep">/</span><b>Processos</b><span class="sep">/</span>Criar campanha no Meta Ads</div>

    <h1>Como criar uma campanha<br>no Meta Ads</h1>
    <p class="sub">O caminho inteiro, da tarefa no ClickUp até o anúncio ativo: qual objetivo escolher,
      qual nomenclatura usar e por quê, e as configurações de campanha, conjunto e anúncio usadas no
      BNCC019. Documentado a partir do lançamento BNCC019 da VOA Conhecimento.</p>

    <div class="pills">
      <span class="pill solid">Meta Ads</span>
      <span class="pill">Lançamento</span>
      <span class="pill">Passo a passo</span>
    </div>
  </div>
</header>

<!-- ROADMAP -->
<section class="sec">
  <div class="wrap">
    <div class="section-label">O mapa</div>
    <h2>As fases do processo, na ordem</h2>
    <p class="intro">Da Fase 0 à Fase 9. Em verde, o que precisa estar pronto para a fase ser dada
      como concluída.</p>

    <div class="road">
      <div class="fase">
        <div class="num">0</div>
        <div>
          <h3>Briefing</h3>
          <p>Tarefa no ClickUp com estrutura, orçamento, público e prazo. Ler a tarefa inteira e
            <strong>todos</strong> os